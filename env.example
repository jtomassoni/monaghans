# Environment Variables

# Database
DATABASE_URL="file:./dev.db"
# For production, use a proper PostgreSQL connection string:
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
# In production, set this to your actual domain:
# NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"
# Generate a secure secret: openssl rand -base64 32

# Google OAuth (for staff/admin login)
# Get these from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID, set authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# For client-side use (optional, for conditional rendering)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

# Superadmin Email (for SSO)
# When logging in via Google OAuth with this email, user will be assigned superadmin role
SUPERADMIN_EMAIL="jtomassoni@gmail.com"

# Role-Based User Credentials (NEW FORMAT - Recommended)
# Format: "username1:password1,username2:password2"
# Multiple users can be defined for each role level, separated by commas
# Each user is defined as "username:password"
# 
# Example: ADMIN_USERS="admin1:securepass123,admin2:anotherpass456"
# Example: OWNER_USERS="owner1:mypassword"
# 
# Available role variables:
# SUPERADMIN_USERS="username1:password1,username2:password2"
# ADMIN_USERS="username1:password1,username2:password2"
# OWNER_USERS="username1:password1"
# MANAGER_USERS="username1:password1"
# COOK_USERS="username1:password1"
# BARTENDER_USERS="username1:password1"
# BARBACK_USERS="username1:password1"
#
# ⚠️  IMPORTANT: At least one role-level credential must be set in production!
# ⚠️  Use strong passwords (at least 8 characters)
# ⚠️  Usernames can be any text (e.g., "jt", "admin", "owner") - no email format required
# ⚠️  Usernames are stored as-is in the database (no domain appended)

# Legacy Admin Credentials (for backward compatibility)
# These are still supported but will be treated as SUPERADMIN_USERS if no new format is set
ADMIN_USERNAME="jt"
# ADMIN_EMAIL="admin@monaghans.com"  # Alternative to ADMIN_USERNAME (kept for backwards compatibility)
ADMIN_PASSWORD="test"
# ⚠️  Note: Legacy format is deprecated. Use role-based arrays above instead.

# Facebook Integration (for automatic posting)
# Get these from: https://developers.facebook.com/apps/
# 1. Create a new Facebook App
# 2. Add "Facebook Login" product
# 3. Add "Pages" product
# 4. Set Valid OAuth Redirect URIs: https://yourdomain.com/api/social/facebook/callback
# 5. Get App ID and App Secret from Settings > Basic
FACEBOOK_APP_ID=""
FACEBOOK_APP_SECRET=""
# Optional: Secret for securing the daily post cron endpoint
CRON_SECRET=""

# Instagram Integration (for future use)
# INSTAGRAM_BUSINESS_ACCOUNT_ID=""
# INSTAGRAM_ACCESS_TOKEN=""

# Stripe Payment Processing
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys for development, live keys for production
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
# Optional: Webhook secret for production (get from Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET="whsec_..."

# Enable/Disable Features
ENABLE_ONLINE_ORDERING="false"
ENABLE_SOCIAL_POSTING="false"

