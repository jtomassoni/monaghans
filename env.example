# Environment Variables

# Database
DATABASE_URL="file:./dev.db"
# For production, use a proper PostgreSQL connection string:
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
# In production, set this to your actual domain:
# NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"
# Generate a secure secret: openssl rand -base64 32

# Google OAuth (for staff/admin login)
# Get these from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID, set authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# For client-side use (optional, for conditional rendering)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

# Superadmin Email (for SSO)
# When logging in via Google OAuth with this email, user will be assigned superadmin role
SUPERADMIN_EMAIL="jtomassoni@gmail.com"

# Admin Credentials (REQUIRED in production)
# These are used to log into the admin panel at /admin/login
# You can use either ADMIN_USERNAME or ADMIN_EMAIL (ADMIN_USERNAME takes precedence)
# Username can be any text (e.g., "jt", "admin", "owner") - no email format required
ADMIN_USERNAME="jt"
# ADMIN_EMAIL="admin@monaghans.com"  # Alternative to ADMIN_USERNAME (kept for backwards compatibility)
ADMIN_PASSWORD="test"
# ⚠️  IMPORTANT: Change these in production!
# ⚠️  Use a strong password (at least 8 characters)
# ⚠️  The system will warn you if you're using the default password in production

# Email Server (for magic link auth - optional)
# EMAIL_SERVER_HOST="smtp.example.com"
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER="your-email@example.com"
# EMAIL_SERVER_PASSWORD="your-password"
# EMAIL_FROM="noreply@yourdomain.com"

# Facebook Integration (for automatic posting)
# Get these from: https://developers.facebook.com/apps/
# 1. Create a new Facebook App
# 2. Add "Facebook Login" product
# 3. Add "Pages" product
# 4. Set Valid OAuth Redirect URIs: https://yourdomain.com/api/social/facebook/callback
# 5. Get App ID and App Secret from Settings > Basic
FACEBOOK_APP_ID=""
FACEBOOK_APP_SECRET=""
# Optional: Secret for securing the daily post cron endpoint
CRON_SECRET=""

# Instagram Integration (for future use)
# INSTAGRAM_BUSINESS_ACCOUNT_ID=""
# INSTAGRAM_ACCESS_TOKEN=""

# Enable/Disable Features
ENABLE_ONLINE_ORDERING="false"
ENABLE_SOCIAL_POSTING="false"

